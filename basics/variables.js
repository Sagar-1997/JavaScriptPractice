const customerId = 12345;
let customerName = "Jhon";
var state = "kanataka";
city = "Banglore";
//customerId=555; not allowd once value is assign in constant we cannot change
//re-declaration of variables not allowed in let and const
//don't use var because it does not provide block scope 
//we can declare let can initialzed after but not possible in const;
console.table([customerId,customerName,state,city,code]);